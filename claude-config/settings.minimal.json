{
  "_comment": "Минимальный settings.json — генерируется core install. После установки модулей будет расширен.",
  "defaultModel": "claude-sonnet-4-6",
  "alwaysThinkingEnabled": true,
  "user": {
    "name": "__USER_NAME__",
    "email": "__USER_EMAIL__"
  },
  "permissions": {
    "_comment_mode": "Чтобы НЕ спрашивало на каждый шаг: нужны И defaultMode=dontAsk, И allow-правила ниже. Один dontAsk без allow Bash НЕ разрешает. bypassPermissions НЕ используем — он ломает telegram-плагин. Хочешь строже — сократи список allow или поставь defaultMode='default'.",
    "defaultMode": "dontAsk",
    "allow": [
      "Bash(*)",
      "Read(*)",
      "Write(*)",
      "Edit(*)",
      "Grep(*)",
      "Glob(*)",
      "WebFetch(*)",
      "WebSearch(*)",
      "Agent(*)",
      "mcp__*"
    ]
  },
  "hooks": {},
  "agentSubpaths": [
    "__HOME__/.claude/agents",
    "__ECC_DIR__/agents"
  ],
  "skillSubpaths": [
    "__HOME__/.claude/skills",
    "__ECC_DIR__/skills"
  ],
  "rulesSubpaths": [
    "__HOME__/.claude/rules",
    "__ECC_DIR__/rules"
  ]
}
